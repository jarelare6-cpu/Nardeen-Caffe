# إصلاحات v41 + v42 — دليل التنفيذ والاختبار

> **نفّذ على فرع `fixes-v41`. لا تدمج في `main` قبل اجتياز خطة الاختبار أدناه.**

```bash
git checkout -b fixes-v41-v42
# انسخ الملفات المرفقة فوق مسارات المشروع
npm install && npm run build      # يجب أن ينجح
```

---

## ١) قبل أي شيء — هجرة قاعدة البيانات

نفّذ في **Supabase ▸ SQL Editor**:

```
db/migrations/2026-08-13_v41_transfer_tron_status.sql     ← أولاً
db/migrations/2026-08-13_v42_supplies_stock_log.sql       ← ثم هذا
```

آمنة تماماً لإعادة التنفيذ. ثم تحقّق:

```sql
SELECT conname, pg_get_constraintdef(oid)
  FROM pg_constraint WHERE conrelid='public.orders'::regclass AND contype='c';

SELECT policyname, cmd FROM pg_policies
 WHERE schemaname='public' AND tablename='orders';
```

**التطبيق يعمل دون الهجرة** (يوجد fallback يُسقط `tron_shift_id` تلقائياً)، لكن دونها:
لن تُحفظ نسبة الإكرامية لوردية إضافتها، وقد يبقى قيد `status` رافضاً للضيافة.

---

## ٢) الملفات المُعدَّلة

| الملف | التغيير |
|---|---|
| `src/lib/store.js` | **كشف التغيير على الصف كاملاً** (السبب الجذري للعطل ٥) + إصلاح `setTables` و `setDebts` |
| `src/lib/utils.js` | اليوم المحاسبي على UTC + دوال الجرد اليومي + نسبة الترون لوردية إضافته |
| `src/lib/rows.js` | عمود `tron_shift_id` مع fallback للقاعدة غير المُرقّاة |
| `src/OperationScreens.jsx` | خريطة طاولات الكاشير + الترون بعد الدفع + استثناء الضيافة من النشط |
| `src/StationScreens.jsx` | إضافة مخزون بكميّة للبار والأراكيل (زيادة فقط) |
| `src/AdminScreens.jsx` | تبويب `ShiftLogTab` + حذف حالة الطاولات المُكرّرة |
| `src/Features.jsx` | **الجرد اليومي لتلغرام من الورديات المُقفلة** + حارس حدّ اليوم |
| `src/HomeScreen.jsx` | تسجيل تبويب «🕐 سجل الورديات» |
| `src/constants.js` | صلاحية `shiftlog` (admin + cashier) |
| `db/migrations/2026-08-13_v41_transfer_tron_status.sql` | **جديد** |

---

## ٣) السبب الجذري — للتوثيق

### العطل ٥ (نقل الطاولات) — والأخطر ممّا بدا

`setOrders` كان يكشف التغيير بقائمة حقول مكتوبة يدوياً:

```js
old.status !== o.status || old.total !== o.total || old.paymentType !== o.paymentType ||
old.discount !== o.discount || (old.tronAmount||0) !== (o.tronAmount||0) ||
JSON.stringify(old.items) !== JSON.stringify(o.items)
```

الحقل `table` **غير موجود**. و`doTransfer` يغيّر `table` فقط ⇒ لا كتابة إلى Supabase ⇒ الخادم يحتفظ بالطاولة القديمة ⇒ الطلبات تعود بعد التحديث.

الحقول المُعطَّلة صامتاً بالسبب نفسه: `table`, `notes`, `customerName`, `workerName`,
`shiftId`, `compAmount`, `isComplimentary`, `partialPaid`, `paidAt`, `stockDeducted`, `originalTotal`.

**الحل:** المقارنة على `rowOfOrder(o)` كاملاً (بلا `updated_at`). يستحيل نسيان عمود مستقبلاً.

نفس العلّة في `setDebts` (كان يقارن `remaining` و `settled` فقط) — **تعديل مبلغ دين أو اسم صاحبه لم يكن يصل القاعدة إطلاقاً.**

### العطل ٤ (الضيافة) — كان في التطبيق لا في القاعدة

`OperationScreens.jsx` مرشّح «نشطة»:

```js
if(filter==="active") return !["paid","cancelled","debt"].includes(o.status);
```

`"complimentary"` ناقصة ⇒ الطلب يُسجَّل في سجل الضيافة **ويبقى معروضاً كطلب نشط**.
وُحِّدت الحالات المُغلقة في `CLOSED_ST` واحد، وأُضيف مرشّح «🎁 ضيافة».

> الضيافة **الجزئية** تُبقي الحالة `"ready"` عمداً — فيها رصيد غير مُقدَّم ضيافةً. هذا سلوك صحيح لا عطل.

### الجرد اليومي لتلغرام — نقص صامت في الإيراد

كان يُبنى من نافذة زمنية: كل ما دُفع منذ بداية اليوم. لكن الوردية الليلية تُفتح
~22:30 UTC وتُقفل 06:00 UTC من الغد ⇒ **طلباتها المدفوعة قبل 00:00 UTC كانت تسقط من الجرد كلياً.**

الآن يُبنى من لقطات الورديات المُقفلة في اليوم المحاسبي — لا نافذة زمنية.

---

## ٤) اليوم المحاسبي — القاعدة المعتمدة

اليوم يبدأ **00:00 بتوقيت غرينتش** (= 3:00 فجراً بتوقيت دمشق).
**الوردية تُنسب لليوم الذي أُقفلت فيه**، لا الذي فُتحت فيه. الوردية المفتوحة لا تدخل أي جرد.

مُطابقة جدولك الفعلي (دمشق = UTC+3):

| الوردية | محلي | UTC | جرد يوم |
|---|---|---|---|
| ليلية | ~01:30 ← 09:00 | 22:30 ← **06:00** | **التالي** ✓ |
| صباحية | 09:00 ← 17:00 | 06:00 ← **14:00** | نفسه ✓ |
| مسائية | 17:00 ← 01:00–02:00 | 14:00 ← **22:00–23:00** | نفسه ✓ |

⇒ يوم UTC واحد = ليلية (امتدّت من الليلة السابقة) + صباحية + مسائية. تماماً كما أردت.
وآخر إقفال في اليوم هو المسائية، فتقرير تلغرام يُرسل عند إقفالها — توقيت صحيح.

### ⚠ الهامش الوحيد الضيّق

المسائية تُقفَل عادةً 01:00–02:00 محلياً، أي **على بُعد 60–120 دقيقة فقط** من نهاية اليوم.
إن تأخّر إقفالها بعد **3:00 فجراً بتوقيت دمشق** انتقل إيرادها كاملاً إلى جرد الغد،
ولن يشمل جرد اليوم الورديات الثلاث.

**الحماية المُضافة:** شاشة تقفيل الوردية تعرض دائماً اليوم الذي ستُحتسب فيه، وتتحوّل
إلى تنبيه برتقالي مع عدّ تنازلي حين يتبقّى ≤ 90 دقيقة.

**للتعديل — سطر واحد** في `src/lib/utils.js`:

```js
export const DAY_START_UTC_HOUR = 0;   // 0 = 3 فجراً دمشق (الحالي، مطابق لطلبك)
```

> لا تجعلها `3`: ستقع نهاية اليوم عند 6:00 UTC = 9:00 صباحاً دمشق — أي **داخل لحظة إقفال الليلية** بالضبط. أسوأ خيار ممكن.

---

## ٥) خطة الاختبار قبل الدمج

### أ) نقل الطاولات (العطل ٥) — الأهم
1. أنشئ طلبين على طاولة 3.
2. الطاولات ▸ 🔀 نقل ▸ من 3 إلى 7 ▸ أشّر الفاتورتين ▸ نفّذ.
3. **حدّث الصفحة (F5).** يجب أن تبقيا على 7.
4. افتح جهازاً آخر — يجب أن تظهرا على 7.
5. تحقّق في Supabase: `SELECT table_num FROM orders WHERE id='...'` ⇒ `7`.

### ب) الضيافة (العطل ٤)
1. طلب جاهز ▸ 🎁 ضيافة كاملة.
2. يختفي فوراً من «الطلبات ▸ نشطة»، ويظهر في «🎁 ضيافة» وفي سجل الضيافة.
3. حدّث الصفحة — **لا يعود**.
4. كرّر مع ☕ مشروب عامل.
5. جرّب ضيافة **جزئية**: يجب أن **يبقى** نشطاً (سلوك صحيح).

### ج) الترون بعد الدفع (العطل ٣)
1. ادفع فاتورة عادية — **لا يوجد زر ترون في شاشة الدفع** (صحيح).
2. أسفل الكاشير ▸ «💠 إضافة ترون على فاتورة مدفوعة» ▸ اختر الفاتورة ▸ 5000 ▸ حفظ.
3. إجمالي الفاتورة **لم يتغيّر**.
4. تقفيل الوردية: «المتوقع بالصندوق» زاد 5000.
5. **الاختبار الحاسم:** أقفل الوردية، افتح وردية جديدة، أضف ترون على فاتورة الوردية السابقة ⇒ يجب أن يظهر في **الوردية الجديدة** (نقده دخل درجها).

### د) حجز الطاولة فور الطلب (العطل ١)
1. أنشئ طلباً على طاولة 5 ولا تُجهّزه.
2. صفحة الكاشير: تظهر «🪑 طاولة 5» فوراً بوسم «⏱ 1 قيد التحضير».
3. البار يُجهّز ⇒ الوسم يتحوّل «✓ 1 جاهز».
4. الدفع ⇒ تختفي الطاولة تلقائياً.

### هـ) كمية البار (العطل ٢)
1. حساب `bar1` ▸ لوحة البار.
2. اضغط `+10` أو اكتب 24 ▸ «＋ إضافة» ⇒ المخزون +24 مرة واحدة.
3. **لا يوجد زر تخفيض** (إلا إن فعّلت `workerCanDecreaseStock`).
4. كرّر في «🧂 مخزون إضافي» وفي لوحة الأراكيل.

### و) سجل الورديات واليوم (٦ و ٧)
1. أقفل الصباحية والمسائية اليوم.
2. لوحة التحكم ▸ 🕐 سجل الورديات ⇒ الورديتان + مجاميع اليوم.
3. افتح وردية ليلية ⇒ تنبيه برتقالي «لا تدخل هذا الجرد».
4. أقفلها صباح الغد 9:00 ⇒ تظهر في جرد **الغد**.
5. جرّب التصدير إلى Excel.
6. غيّر توقيت جهازك (مثلاً إلى UTC−5) ⇒ **الأرقام لا تتغيّر** (هذا معنى UTC).

---

## ٦) خطة التراجع

```bash
git checkout main            # الكود يعود فوراً
```

الهجرة **غير هدّامة**: `tron_shift_id` عمود جديد فقط، والقيد الجديد أوسع من القديم.
لا حاجة للتراجع عنها. إن أردت مع ذلك:

```sql
ALTER TABLE public.orders DROP CONSTRAINT IF EXISTS orders_status_check;
-- (لا تحذف tron_shift_id — لا ضرر منه ويحمل بيانات إكراميات)
```

---

---

# v42 — المخزون: جدول مستقلّ + أثر تدقيق + تدوير اليوم

## أ) فقدان التحديث الصامت — الحل الجذري

مواد البار كانت في `app_settings.data.extraStock`: **حقل JSONB واحد**.
كل تعديل يكتب المصفوفة كاملة ⇒ جهازان يعدّلان معاً ⇒ آخر كتابة تمحو الأخرى بلا خطأ.

نفس العلّة كانت في مخزون المنيو: التطبيق يرسل **القيمة المطلقة** (`stock = 30`).
جهازان يقرآن 25 ويضيف كلٌّ 5 ⇒ كلاهما يرسل 30 ⇒ الناتج **30 بدل 35**.
وحدة بضاعة ضاعت بلا أثر.

**الحل:** جدول `supplies` مستقلّ + **إرسال الفارق لا القيمة**.
دالتا `adjust_stock` و `adjust_supply` تُجريان `qty = qty + delta` على الصف
المقفول داخل القاعدة ⇒ `25 + 5 + 5 = 35` مهما تزامنت الأجهزة.

`p_move_id` حتمي ⇒ إعادة الإرسال بعد انقطاع لا تُضاعف الكمية (idempotent).
وإن لم تُنفَّذ الهجرة بعد، يرتدّ التطبيق للمسار القديم تلقائياً ويظلّ يعمل.

## ب) سجل حركات المخزون

`stock_movements` يسجّل: **من، ماذا، كم، الرصيد بعدها، لماذا، متى، أي وردية**.
يُكتب داخل **نفس معاملة** تعديل الرصيد ⇒ يستحيل أن يتغيّر مخزون بلا حركة مقابلة.
سياسة RLS تسمح بالقراءة والإضافة فقط — **لا تعديل ولا حذف**.

تبويب جديد: **📦 سجل المخزون** (أدمن فقط) مع مرشّحات وتصدير Excel.

## ج) الترحيل

المواد القديمة تُنقل تلقائياً من `extraStock` إلى `supplies` عند أول تحميل
يجد الجدول فارغاً (وملف الهجرة يفعل الشيء نفسه — أيّهما أسبق).
**`settings.extraStock` يبقى كما هو** كنسخة احتياطية.

## د) تدوير اليوم عند 00:00 غرينتش

> **لا شيء يُقفَل تلقائياً.** الورديات تُفتح وتُقفل يدوياً من الكاشير كما طلبت.
> الساعة 0 UTC حدٌّ **حسابي**: دفتر اليوم يُقفَل ويبدأ دفتر جديد.

**المسار الأساسي:** التقرير يُرسل عند إقفال المسائية (~22:30 UTC) — آخر وردية
تُقفَل في اليوم، فتكون الثلاث قد اكتملت.

**شبكة الأمان (v42):** إن لم يُرسَل جرد يومٍ ما — نُسي الإقفال، أو أُقفلت
المسائية بعد منتصف ليل غرينتش، أو كانت كل الأجهزة مغلقة — يُرسَل تلقائياً
بعد انقضاء اليوم عند أول اتصال. يعمل على أجهزة الأدمن/الكاشير فقط،
ويُختم بـ `settings.lastDailySent` المشترك فلا يتكرّر بين الأجهزة.
ولا يُرسِل جرداً ناقصاً: إن كانت وردية من ذلك اليوم ما زالت مفتوحة، ينتظر إقفالها.

## هـ) ملفات v42

| الملف | الحالة |
|---|---|
| `src/lib/stockLog.js` | **جديد** — مُحوِّلات المواد والحركات + الترحيل + التلخيص |
| `src/lib/dailyReport.js` | **جديد** — بناء الجرد اليومي + شبكة أمان التدوير |
| `src/lib/supabase.js` | `adjustStockAtomic` / `adjustSupplyAtomic` + اشتراكات حيّة |
| `src/lib/store.js` | حالة `supplies` و `stockMoves` + `adjustStock` / `adjustSupply` + الترحيل |
| `src/StationScreens.jsx` | البار والأراكيل على المسار الذرّي وجدول `supplies` |
| `src/AdminScreens.jsx` | تبويب `StockLogTab` |
| `src/HomeScreen.jsx` | تسجيل التبويب + مؤقّت تدوير اليوم |
| `src/Features.jsx` | يستخدم `buildDailyPacket` المشترك ويختم `lastDailySent` |
| `src/constants.js` | صلاحية `stocklog` (أدمن فقط) |
| `db/migrations/2026-08-13_v42_supplies_stock_log.sql` | **جديد** |

## و) اختبار v42

### المخزون الذرّي — الاختبار الحاسم
1. افتح **جهازين** على لوحة البار، نفس الصنف (رصيد 25).
2. اضغط `+5` على الجهازين **في نفس اللحظة**.
3. الرصيد يجب أن يصبح **35** لا 30.
4. 📦 سجل المخزون ⇒ **حركتان** منفصلتان، كلٌّ بصاحبها ووقتها.

### المواد الإضافية
1. أول تحميل بعد الهجرة: المواد القديمة موجودة كما هي.
2. أضف مادة من جهاز ⇒ تظهر فوراً على الجهاز الآخر (مزامنة حيّة).
3. عدّل من جهازين معاً ⇒ **لا فقدان**.

### شبكة أمان الجرد
1. أقفل ورديات يوم كامل دون إقفال المسائية (أو أقفلها بعد 3 فجراً).
2. اترك جهاز الأدمن مفتوحاً ⇒ خلال ≤10 دقائق بعد انقضاء اليوم يصل الجرد.
3. لا يتكرّر على جهاز آخر (`lastDailySent`).

---

## ٨) ملاحظات مفتوحة (لم تُنفَّذ)

1. **التقرير الأسبوعي** ما زال يُبنى بنافذة زمنية وفيه نفس نقص الليلية.
   حُصر نطاق هذه الدفعة عمداً. يُنقل لأساس الورديات في v43.

2. **خصم المخزون عند البيع** لا يمرّ بعد عبر `stock_movements`
   (`deductOrderStock` يعمل كما كان). السجل يغطّي الآن حركات البار والأراكيل
   والتصحيح. توحيد مسار البيع خطوة v43.

3. **كلمات المرور ما زالت منشورة في `README.md`** في مستودع عام.
   غيّرها واحذف الجدول.

---

# v43 — التدقيق، التنظيم، ونهاية البتر الصامت

## أ) البتر الصامت — أخطر ما وُجد في التدقيق

`store.js` يجلب **آخر 500 طلب**. ولوحة التحكم كانت تحسب عليها ثم تكتب «كل الوقت».
كافيه يبيع 60 طلباً يومياً يستنفد الـ500 في **ثمانية أيام** — فالرقم الذي تقرأه
كإجمالي مشروعك هو إجمالي أسبوع ونصف. والأرشفة (90 يوماً) تزيد النقص.
لا خطأ ولا تحذير: الرقم يبدو سليماً وهو ناقص.

**الحل:** تجميعات في القاعدة (`sales_totals`, `cogs_total`, `expenses_totals`,
`staff_performance`) تشمل `orders` و`orders_archive` معاً.

**والمبدأ الحاكم:** رقم صحيح، أو رقم موسوم بأنه جزئي — **لا رقم كاذب**.
كل نتيجة تحمل `exact`. عند تعذّر التجميع يُعرض «≈» مع توضيح صريح.

## ب) لوحة التحكم — أربعة أقسام صريحة

كانت تسع بطاقات متجاورة: مالي وتشغيلي وتحذيري في صفٍّ واحد. الآن:

| القسم | المحتوى |
|---|---|
| 🗓 **يوم العمل** | التاريخ المحاسبي + حالة كل وردية (مُقفلة/مفتوحة) |
| 💰 **الإيراد** | المبيعات · نقدي · بطاقة · ترون · متوسط الفاتورة |
| 📒 **النفقات والالتزامات** | مصاريف · تكلفة البضاعة · ضيافة · ديون · صافي الربح |
| 📋 **سجل العمل** | معلّقة · تحضير · جاهزة · طاولات · زمن التحضير + آخر حركات المخزون |
| 💎 **الإجماليات التراكمية** | من القاعدة، تشمل الأرشيف |

**وأُصلحت ثلاث علل في اللوحة:**
1. الترويسة كانت تعرض تاريخ الجهاز المحلي فوق أرقام يوم غرينتش — فبين منتصف
   الليل و3 فجراً دمشق (قلب الوردية الليلية) يظهر عنوان يوم والأرقام ليوم آخر.
2. الرسم البياني كان بساعات الجهاز، فنافذته الاثنتا عشرة ساعة تعبر يومين محاسبيين.
   صار 24 ساعة غرينتش داخل اليوم المحاسبي الواحد.
3. `key` مشتقّ من عدد الطلبات كان **يُعيد تركيب اللوحة كلياً** عند كل طلب أو دفعة.
   أُزيل، وأُضيف `useMemo` لكل الحسابات (كانت ~10 مسحات كاملة عند كل عرض).

## ج) توحيد خصم المخزون

البيع كان يخصم بكتابة **قيمة مطلقة** من المتصفح وخارج سجل الحركات:
جهازان يبيعان الصنف نفسه معاً ⇒ خصم واحد يُمحى. ولا أثر يُدقَّق.

الآن `adjust_stock_bulk`: خصم نسبي ذرّي لكل أصناف الطلب في نداء واحد، مع حركة
لكل صنف. معرّف الحركة مشتقّ من (الطلب + الصنف + السبب) **داخل القاعدة**،
فيستحيل الخصم المزدوج حتى مع إعادة الإرسال بعد انقطاع.

توقيع `deductOrderStock(store, order)` محفوظ — أُضيف وسيط `meta` اختياري فقط.

## د) الورديات — صفحة واحدة

`تقفيل الوردية` + `سجل الورديات` + `أداء الموظفين` في تبويب واحد **🕐 الورديات**،
وحلّ محلّ «الحديقة أدمن» في الشريط الأساسي (الحديقة انتقلت إلى «المزيد»).

**حارس الوردية المزدوجة:** فهرس فريد في القاعدة
(`shifts_one_open_per_branch`) + فحص في الواجهة برسالة مفهومة.
جهازان يفتحان وردية معاً كان ينتج ورديتين مفتوحتين فتتوزّع الطلبات بينهما
عشوائياً وينهار جرد الصندوق.

**تنبيه الوردية الطويلة:** إن تجاوزت `settings.shiftMaxHours` (12 افتراضياً).

## هـ) الجرد الدوري

📦 المخزون ▸ **🔢 الجرد الدوري**: تُدخِل الكمية الفعلية على الرفّ، فيُسجَّل الفارق
عن رصيد النظام كحركة `correction` باسمك. هذا ما يكشف الهدر — النظام لا يعرف إلا
ما سُجّل، والفرق هو الرقم الوحيد الذي يحكي ما لم يُسجَّل.

## و) أداء الموظفين

من لقطات الورديات المُقفلة (سجل كامل غير مبتور): مبيعات، نقدي، ترون، ضيافة،
ديون، مصاريف، متوسط الفاتورة، و**فروقات الصندوق**: الصافي والمطلق وأسوأ عجز.

> المطلق يكشف ما يخفيه الصافي: عجز 2000 ثم زيادة 1000 صافيه −1000 لكن مطلقه 3000.

⚠️ الفروقات **مؤشّر لا إثبات** — قد تنشأ من خطأ عدّ أو مصروف غير مسجَّل.

## ز) رقم النسخة

`package.json` ▸ `"version": "43"` ⇒ الشاشة تعرض **v43**.

## ح) ملفات v43

| الملف | الحالة |
|---|---|
| `src/lib/aggregates.js` | **جديد** — تجميعات الخادم + الوسم الصادق |
| `db/migrations/2026-08-13_v43_aggregates_stock_shift.sql` | **جديد** |
| `src/lib/stock.js` | الخصم/الإرجاع عبر المسار الذرّي المُسجَّل |
| `src/lib/store.js` | `applyOrderStock` |
| `src/lib/supabase.js` | `adjustStockBulkAtomic` |
| `src/AdminScreens.jsx` | لوحة التحكم مُعاد بناؤها + `StockCountView` + `StaffPerformanceView` |
| `src/Features.jsx` | حارس الوردية المزدوجة + تنبيه الوردية الطويلة |
| `src/HomeScreen.jsx` | `ShiftsPage` الموحّدة + إزالة `key` اللوحة |
| `src/OperationScreens.jsx` | تمرير المستخدم والوردية لسجل الحركات |
| `src/NardeenIcons.jsx` | أيقونتا `shifts` و`stocklog` |
| `src/uikit.jsx` | الشريط الأساسي: الورديات بدل الحديقة |
| `src/constants.js` | صلاحية `shifts` |
| `package.json` | v43 |

## ط) اختبار v43

1. **الوردية المزدوجة:** جهازان ▸ فتح وردية على «الكافيه» معاً ⇒ واحد ينجح والآخر يرى رسالة واضحة.
2. **خصم المخزون:** ادفع فاتورة ⇒ 📦 سجل الحركات يُظهر حركة لكل صنف بسبب «بيع» واسم الكاشير.
3. **لا خصم مزدوج:** اقطع الشبكة، ادفع، أعد الاتصال ⇒ حركة واحدة لا اثنتان.
4. **الجرد الدوري:** أدخل كمية فعلية مخالفة ⇒ تظهر في المعاينة ⇒ سجّل ⇒ حركة «تصحيح جرد».
5. **الإجماليات:** بعد الهجرة تختفي «≈» من الإجماليات التراكمية. قبلها تظهر مع توضيح.
6. **التوقيت:** غيّر توقيت الجهاز ⇒ ترويسة «يوم العمل» والأرقام **لا تتغيّر**.


---

## ⚠️ تصحيح ملف هجرة v43

النسخة الأولى فشلت بخطأ:
`ERROR: 42601 each UNION query must have the same number of columns`

**السبب:** `orders_archive` أُنشئ بـ `LIKE public.orders` في وقت سابق، ثم أضفنا
`tron_shift_id` في v41 إلى `orders` وحدها — فصار الجدولان بعدد أعمدة مختلف،
و`UNION ALL` مع `SELECT *` يشترط التطابق.

**الحل مزدوج:**
1. مزامنة أعمدة الأرشيف (`ADD COLUMN IF NOT EXISTS`).
2. تسمية الأعمدة **صراحةً** في كل `UNION` بدل `SELECT *` — فلا يتكرّر العطل
   مع أي عمود يُضاف مستقبلاً.

### وخطأ حسابي أخطر في النسخة الأولى

`cogs_total` كان يحسب:

```sql
qty - compQty * CASE WHEN complimentary THEN 0 ELSE 1 END
```

أسبقية `*` أعلى من `-`، فالسطر المُقدَّم ضيافةً كاملةً كان ناتجه `qty - 0 = qty`
— أي **تُحتسب تكلفته كاملةً** بدل استثنائها. عكس قاعدة `orderCogs` تماماً،
فيظهر صافي الربح التراكمي أقلّ من الحقيقة بمقدار تكلفة كل الضيافة.

القاعدة الصحيحة الآن مطابِقة للتطبيق:
* سطر ضيافة كاملة ⇒ التكلفة صفر.
* غير ذلك ⇒ `GREATEST(0, qty − compQty)`.

تحقّقتُ منها بمحاكاة دلالة SQL في JS ومقارنتها بـ `orderCogs` على سبع حالات
(عادي، ضيافة كاملة، جزئية، جزئية تعادل الكل، صنف بلا تكلفة، كميات صفرية، مزيج)
— تطابق تام، مع إثبات أن النسخة القديمة كانت تعطي 300 حيث يجب أن تعطي 0.

**استبدل ملف SQL بالنسخة المصحّحة ونفّذها. لا تغيير في ملفات التطبيق.**
