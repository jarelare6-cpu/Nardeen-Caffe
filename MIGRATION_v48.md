# ناردين كافيه — النسخة 48

رقم النسخة يظهر الآن **v48** بجانب اسم الكافيه في ترويسة التطبيق
(مصدره `package.json` عبر `__APP_VERSION__`).

---

## أولاً: الهجرة (قبل رفع الكود)

```
db/migrations/2026-08-16_v48_shift_fingerprint.sql
```

آمنة لإعادة التنفيذ. تضيف عمود `fingerprint` إلى جدول `shifts`.

> **تحقّق من هجرة v47.2 أيضاً** — الاستعلام (ج) في نهاية ملف v48 يخبرك
> إن كان جدول `report_log` موجوداً. إن لم يكن، نفّذ
> `2026-08-16_v47.2_report_lock.sql` أولاً.

---

## ١) عطل تلغرام — الجذر والحل النهائي

### السبب الذي فات الجولات السابقة

في `dailyReport.js` كان الفحص:

```js
if ((settings?.lastDailySent || "") === dayKey) return false;
```

`lastDailySent` يعني «كل الأيام حتى هذا التاريخ أُرسلت» — لا «هذا اليوم
الوحيد أُرسل». المقارنة بالتساوي كانت تُمرّر كل يوم آخر. وحلقة التصريف
تختار **الأقدم** المؤهَّل ثم تختم به، فيتحرّك الختم **إلى الوراء**:

```
الختم: 2026-08-15  (أي 13 و14 و15 أُرسلت)
نبضة 1: أرسل 13  →  الختم صار 13
نبضة 2: أرسل 14  →  الختم صار 14
نبضة 3: أرسل 13  →  الختم صار 13
نبضة 4: أرسل 14  →  ...
```

**تناوب لا نهائي بين يومين قديمين.** هذا ما كنت تراه.

### الحل — طبقتان

**(أ) الختم أحادي الاتجاه**
`isDayStamped()` تقارن `dayKey <= lastDailySent`، و`advanceStamp()` تضمن
أن الختم لا يتراجع أبداً. المحاكاة: من تناوب لا نهائي إلى **صفر إرسال**.

**(ب) إلغاء الشبكة التلقائية نهائياً**
حُذف المؤقّت من `HomeScreen.jsx` بالكامل. منه خرجت كل أعطال تلغرام
الثلاثة: التكرار بين الأجهزة، حلقة إعادة التسليح، إعادة الأيام القديمة.
عولجت الأعراض ثلاث مرات وعادت في كل مرة بشكل جديد — لأن الجذر لم يكن
عطلاً بعينه، بل وجود قرار تلقائي مؤقَّت أصلاً.

**البديل: تبويب «🔏 تدقيق الورديات» ← زرّ «أرسل جرد يوم…»**
- تختار اليوم من قائمة الأيام المُقفلة (المُرسَل موسوم بـ ✓).
- معاينة قبل الإرسال: الورديات، المبيعات، فرق الصندوق، عدد الطلبات.
- يرفض الإرسال مع سبب واضح إن كان اليوم جارياً أو فيه وردية مفتوحة.
- «إعادة إرسال متعمّدة» لتكرار يوم أُرسل — بقرار صريح لا بالصدفة.
- كل إرسال يُسجَّل في `activity_log` و`report_log`.

> الإرسال التلقائي عند إقفال **آخر وردية في اليوم** يبقى كما هو — وهو
> المسار الأساسي الصحيح، ومحميّ بالحجز الذرّي في `report_log`.

---

## ٢) بصمة الوردية (جديد)

بصمة تشفيرية SHA-256 تُحسَب لحظة الإقفال على محتوى الوردية وتُخزَّن معها.
إعادة حسابها لاحقاً تُثبت رياضياً هل مُسّت الأرقام بعد الإقفال.

**أربع بصمات جزئية لا واحدة:** الطلبات · أرقام الصندوق · المصاريف ·
حركات المخزون. بصمة واحدة تقول «تغيّر شيء»؛ أربع تقول «تغيّرت الطلبات
والصندوق سليم» — فتذهب مباشرةً إلى موضع الخلل.

الاختبارات تكشف: تعديل مبلغ طلب · حذف طلب · تعديل المعدود · إضافة مصروف
· تعديل حركة مخزون. ولا تتأثر بترتيب المصفوفات (حتمية تماماً).

عند كشف تغيير: تنبيه أحمر + تسجيل في `activity_log` + عرض أي جزء تغيّر
مع فرق العدد (مثلاً «الطلبات — العدد 2 ← 1»).

> **حدّ صريح:** هذه بصمة **كاشفة** للتغيير لا مانعة له، وليست توقيعاً
> رقمياً. مَن يملك صلاحية الكتابة على القاعدة يستطيع إعادة حسابها.
> قيمتها في كشف التعديل العَرَضي وغير الموثَّق — وهو ما يحدث فعلاً.

الورديات المُقفلة قبل v48 تظهر «بلا بصمة» — ولا تُبصَم بأثر رجعي، لأن
بصمة تُحسب اليوم على بيانات قد تكون تغيّرت لا تُثبت شيئاً.

---

## ٣) إصلاحات محاسبية

**توحيد «صافي الربح».** كان التطبيق يحمل تعريفين متناقضين بنفس التسمية:

| الموضع | قبل | بعد |
|---|---|---|
| لوحة اليوم | مبيعات − تكلفة − مصاريف | كما هو |
| الإجماليات التراكمية | مبيعات − تكلفة **فقط** | مبيعات − تكلفة − **مصاريف** |

الرقم الثاني كان يبالغ في الربح بمقدار كل المصاريف.

**مهلة الجلسة حسب الدور.** كانت 30 دقيقة للجميع فتُخرج الكاشير وسط
ورديته في ساعات الهدوء. الآن: **8 ساعات** للكاشير والأدمن (جهاز ثابت
داخل الكافيه)، و30 دقيقة لبقية الأدوار.
قابلة للضبط: `settings.sessionTimeoutMinutes`.

---

## ⚠ ملاحظة محاسبية لم تُصلَح بالكود (ولا يمكن)

**تكلفة كل الأصناف = صفر.** لا يوجد حقل `cost` في المنيو الافتراضي، فـ
`orderCogs` تُرجع صفراً دائماً و«صافي الربح» = المبيعات − المصاريف.

هذا ما رأيته في جردَي ٩ و١٠ آب: `6,400 − 620 = 5,780` بالضبط.

**أدخل تكلفة كل صنف من شاشة المنيو** — عندها فقط يصبح رقم الربح صحيحاً
في الجرد والتقارير كلها. لا يمكن للكود أن يخترع هذه الأرقام.

---

## الملفات

| ملف | الحالة |
|---|---|
| `db/migrations/2026-08-16_v48_shift_fingerprint.sql` | **جديد** — إلزامي |
| `src/lib/fingerprint.js` | **جديد** — محرّك البصمة |
| `src/ShiftAudit.jsx` | **جديد** — الزرّ اليدوي + التحقّق من البصمة |
| `src/lib/dailyReport.js` | الختم أحادي الاتجاه · `canSendDaily` |
| `src/HomeScreen.jsx` | حذف المؤقّت التلقائي · تبويب التدقيق |
| `src/Features.jsx` | حساب البصمة عند الإقفال · ختم أحادي |
| `src/lib/store.js` | مهلة الجلسة حسب الدور · قراءة البصمة |
| `src/lib/rows.js` | حفظ البصمة في القاعدة |
| `src/AdminScreens.jsx` | توحيد صافي الربح |
| `src/constants.js` | صلاحية تبويب التدقيق |
| `package.json` | الإصدار 48 |

---

## الخطوات

1. نفّذ هجرة v48 (وتحقّق من v47.2).
2. فُكّ الضغط فوق جذر المستودع.
3. **`npm run build`** — لم أستطع تشغيله (بيئتي بلا اتصال بمستودع الحزم).
4. ارفع، ثم أغلق تطبيق PWA وأعد فتحه على كل جهاز.
5. تحقّق: الترويسة تُظهر **v48**.

## بعد النشر — تحقّق ميداني

- افتح «🔏 تدقيق الورديات» ← يجب أن يعمل زرّ الإرسال اليدوي.
- أقفل وردية ← يجب أن تظهر بصمتها في القائمة (16 حرفاً).
- اضغط «تحقّق» ← يجب أن تظهر «✅ مطابقة».
- راقب أن تلغرام **لا يرسل شيئاً تلقائياً** إلا عند إقفال آخر وردية.

```sql
SELECT id, sent_at, sent_by, device FROM report_log ORDER BY sent_at DESC LIMIT 20;
```
